services:
  - type: web
    name: contentus
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: CONTENTUS_MODE
        value: production
      - key: SKIP_INSTALL_DEPS
        value: true
